    // Section 6: Vibe Coding â€” AI-Powered Live Code Editor Mockup
    const VibeCodingSection = () => {
      const containerRef = useRef(null);
      const [mousePos, setMousePos] = useState({ x: 0, y: 0 });
      const [activeFile, setActiveFile] = useState('App.jsx');
      const [promptText, setPromptText] = useState('');
      const [isGenerating, setIsGenerating] = useState(false);
      const [generatedLine, setGeneratedLine] = useState(0);

      const handleMouseMove = (e) => {
        const rect = containerRef.current.getBoundingClientRect();
        setMousePos({ x: e.clientX - rect.left, y: e.clientY - rect.top });
      };

      const handleGenerate = () => {
        if (isGenerating) return;
        setIsGenerating(true);
        setGeneratedLine(0);
        let line = 0;
        const interval = setInterval(() => {
          line++;
          setGeneratedLine(line);
          if (line >= 12) { clearInterval(interval); setIsGenerating(false); }
        }, 120);
      };



      const colorMap = {
        keyword: '#c792ea', fn: '#82aaff', name: '#ffcb6b',
        string: '#c3e88d', comment: '#546e7a', brace: '#89ddff',
        tag: '#f07178', attr: '#ffcb6b', op: '#89ddff',
        plain: '#eeffff'
      };

      const fileTree = ['App.jsx', 'HeroCard.jsx', 'styles.css', 'prompt.txt'];

      const terminalLines = [
        { c: '#4ec9b0', t: '$ vibe-engine --watch' },
        { c: '#9cdcfe', t: '[AI] Scanning prompt: "build a hero card component"' },
        { c: '#4ec9b0', t: '[AI] Intent parsed â†’ React component, hooks, tailwind' },
        { c: '#ce9178', t: '[GEN] Streaming tokens... 98 tokens/sec' },
        { c: '#4ec9b0', t: '[OK]  HeroCard.jsx written âœ“  (312ms)' },
        { c: '#dcdcaa', t: '[HOT] localhost:5173 â†’ live reload triggered' },
      ];

      return (
        <section
          id="vibe-coding"
          className="relative min-h-screen py-32 px-6 md:px-12 lg:px-24 bg-brand-bg border-t border-white/5 flex flex-col justify-center select-none"
        >
          {/* Ambient glow blob */}
          <div className="absolute top-1/2 right-[10%] -translate-y-1/2 w-[400px] h-[400px] bg-violet-600/8 filter blur-[120px] rounded-full pointer-events-none z-0" />
          <div className="absolute bottom-[20%] left-[5%] w-[250px] h-[250px] bg-brand-cyan/5 filter blur-[90px] rounded-full pointer-events-none z-0" />

          <div className="max-w-7xl mx-auto w-full grid grid-cols-1 lg:grid-cols-12 gap-16 items-center relative z-10">

            {/* Left: Code editor mockup */}
            <div
              ref={containerRef}
              onMouseMove={handleMouseMove}
              style={{ '--mouse-x': `${mousePos.x}px`, '--mouse-y': `${mousePos.y}px` }}
              className="lg:col-span-7 glow-card glass-panel rounded-3xl border border-white/10 bg-slate-950/20 backdrop-blur-md overflow-hidden h-[520px] md:h-[560px] flex flex-col"
            >
              <div className="glow-card-bg" />

              {/* Editor chrome â€” traffic lights + tabs */}
              <div className="relative z-10 flex items-center justify-between px-4 py-3 border-b border-white/5 bg-slate-950/60 flex-shrink-0">
                <div className="flex items-center space-x-2">
                  <span className="w-3 h-3 rounded-full bg-red-500/80" />
                  <span className="w-3 h-3 rounded-full bg-yellow-400/80" />
                  <span className="w-3 h-3 rounded-full bg-emerald-500/80" />
                </div>
                {/* File tabs */}
                <div className="flex items-center space-x-1">
                  {fileTree.map(f => (
                    <button
                      key={f}
                      onClick={() => setActiveFile(f)}
                      className={`px-3 py-1 text-[10px] font-mono rounded transition-all duration-200 ${
                        activeFile === f
                          ? 'bg-brand-cyan/15 border border-brand-cyan/30 text-brand-cyan'
                          : 'text-gray-500 hover:text-gray-300'
                      }`}
                    >{f}</button>
                  ))}
                </div>
                <div className="flex items-center space-x-1.5">
                  {isGenerating && (
                    <span className="text-[9px] font-mono text-violet-400 animate-pulse uppercase tracking-widest">Generatingâ€¦</span>
                  )}
                  <span className="w-1.5 h-1.5 rounded-full bg-emerald-400 animate-pulse" />
                  <span className="text-[9px] font-mono text-gray-500">Vibe Engine v2.1</span>
                </div>
              </div>

              {/* Main editor area split */}
              <div className="relative z-10 flex flex-grow overflow-hidden">

                {/* Line numbers gutter */}
                <div className="w-10 flex-shrink-0 bg-slate-950/50 border-r border-white/5 flex flex-col pt-3 pb-3 items-end pr-2">
                  {Array.from({ length: 18 }).map((_, i) => (
                    <span key={i} className="text-[10px] font-mono text-gray-600 leading-5">{i + 1}</span>
                  ))}
                </div>

                {/* Code content */}
                <div className="flex-grow p-3 overflow-hidden text-[11px] font-mono leading-5 relative">
                  {activeFile === 'App.jsx' ? (
                    <div>
                      {/* Static lines always visible */}
                      <div>
                        <span style={{ color: colorMap.keyword }}>import </span>
                        <span style={{ color: colorMap.brace }}>{'{ useState, useEffect }'}</span>
                        <span style={{ color: colorMap.keyword }}> from </span>
                        <span style={{ color: colorMap.string }}>'react'</span>
                      </div>
                      <div style={{ color: colorMap.comment }}>{'// AI generated component â€” DXIGN Vibe Engine'}</div>
                      <div className="mt-1">
                        <span style={{ color: colorMap.keyword }}>export default </span>
                        <span style={{ color: colorMap.fn }}>function </span>
                        <span style={{ color: colorMap.name }}>HeroCard</span>
                        <span style={{ color: colorMap.brace }}>{'() {'}</span>
                      </div>
                      {/* Animated streamed lines */}
                      {generatedLine >= 1 && <div className="ml-4"><span style={{ color: colorMap.keyword }}>const </span><span style={{ color: colorMap.name }}>[count, setCount]</span><span style={{ color: colorMap.op }}> = </span><span style={{ color: colorMap.fn }}>useState</span><span style={{ color: colorMap.brace }}>(0)</span></div>}
                      {generatedLine >= 2 && <div className="mt-1 ml-4"><span style={{ color: colorMap.keyword }}>return </span><span style={{ color: colorMap.tag }}>{'<div '}</span><span style={{ color: colorMap.attr }}>className</span><span style={{ color: colorMap.op }}>=</span><span style={{ color: colorMap.string }}>"hero-card"</span><span style={{ color: colorMap.tag }}>{'>'}</span></div>}
                      {generatedLine >= 3 && <div className="ml-8"><span style={{ color: colorMap.tag }}>{'<h1 '}</span><span style={{ color: colorMap.attr }}>onClick</span><span style={{ color: colorMap.op }}>=</span><span style={{ color: colorMap.brace }}>{'{'}</span><span style={{ color: colorMap.fn }}>{'() => setCount(c => c+1)'}</span><span style={{ color: colorMap.brace }}>{'}'}</span><span style={{ color: colorMap.tag }}>{'>'}</span><span style={{ color: colorMap.plain }}>DXIGN Ã— AI</span><span style={{ color: colorMap.tag }}>{'</h1>'}</span></div>}
                      {generatedLine >= 4 && <div className="ml-8"><span style={{ color: colorMap.tag }}>{'<p>'}</span><span style={{ color: colorMap.plain }}>Clicks: </span><span style={{ color: colorMap.brace }}>{'{count}'}</span><span style={{ color: colorMap.tag }}>{'</p>'}</span></div>}
                      {generatedLine >= 5 && <div className="ml-4"><span style={{ color: colorMap.tag }}>{'</div>'}</span></div>}
                      {generatedLine >= 6 && <div><span style={{ color: colorMap.brace }}>{'}'}</span></div>}
                      {/* Blinking cursor */}
                      <span className="inline-block w-2 h-4 bg-brand-cyan/80 ml-0.5 animate-pulse align-middle" />
                    </div>
                  ) : (
                    <div className="text-gray-600 text-[10px] mt-4 text-center">
                      <Code2 size={28} className="mx-auto mb-2 text-gray-700" />
                      <div>Select a file to view</div>
                    </div>
                  )}
                </div>
              </div>

              {/* Bottom: Terminal / prompt strip */}
              <div className="relative z-10 border-t border-white/5 bg-slate-950/80 flex-shrink-0">
                {/* Terminal output panel */}
                <div className="px-4 py-2 space-y-0.5 max-h-[88px] overflow-hidden">
                  {terminalLines.slice(0, generatedLine > 0 ? Math.min(generatedLine + 1, terminalLines.length) : 1).map((l, i) => (
                    <div key={i} className="text-[9px] font-mono leading-4" style={{ color: l.c }}>{l.t}</div>
                  ))}
                </div>
                {/* Prompt input */}
                <div className="flex items-center px-4 py-2 border-t border-white/5 space-x-3">
                  <Sparkles size={12} className="text-violet-400 flex-shrink-0" />
                  <input
                    type="text"
                    value={promptText}
                    onChange={e => setPromptText(e.target.value)}
                    onKeyDown={e => e.key === 'Enter' && handleGenerate()}
                    placeholder='Try: "build a hero card component"'
                    className="flex-grow bg-transparent text-[10px] font-mono text-gray-300 placeholder-gray-600 outline-none caret-brand-cyan"
                  />
                  <button
                    onClick={handleGenerate}
                    className="px-3 py-1 rounded text-[9px] font-mono font-bold uppercase tracking-wider bg-violet-600/20 border border-violet-500/30 text-violet-300 hover:bg-violet-600/40 hover:text-white transition-all duration-200 flex-shrink-0"
                  >Generate</button>
                </div>
              </div>
            </div>

            {/* Right: Copy */}
            <div className="lg:col-span-5 flex flex-col text-left">
              <span className="text-xs font-mono text-brand-cyan font-bold uppercase tracking-widest flex items-center mb-3">
                <span className="w-1.5 h-1.5 bg-brand-cyan rounded-full mr-2 animate-pulse" />
                Module 06 / Vibe Coding
              </span>
              <h2 className="text-4xl md:text-5xl lg:text-6xl font-extrabold uppercase text-white font-heading tracking-tight leading-none mb-6">
                BUILD WITH<br/>
                <span className="text-cyan-gradient">AI PROMPTS.</span>
              </h2>
              <p className="text-gray-400 text-sm md:text-base leading-relaxed mb-8 max-w-lg">
                Describe your idea in plain English. The DXIGN Vibe Engine translates intent into clean, production-ready code â€” React components, APIs, animations â€” streamed live into your editor with zero boilerplate.
              </p>

              {/* Feature badges */}
              <div className="grid grid-cols-2 gap-3 mb-8">
                {[
                  { icon: <Code2 size={14} />, label: 'React & Next.js', sub: 'Component generation' },
                  { icon: <Sparkles size={14} />, label: 'Prompt to UI', sub: 'Instant visual output' },
                  { icon: <Cpu size={14} />, label: 'API scaffolding', sub: 'Routes & handlers' },
                  { icon: <Zap size={14} />, label: 'Hot reload', sub: 'Live browser preview' },
                ].map((item, idx) => (
                  <div key={idx} className="p-3 rounded-xl border border-white/5 bg-white/[0.02] hover:border-brand-cyan/20 hover:bg-brand-cyan/[0.03] transition-all duration-300 flex items-start space-x-3">
                    <span className="text-brand-cyan mt-0.5 flex-shrink-0">{item.icon}</span>
                    <div>
                      <div className="text-white text-xs font-semibold">{item.label}</div>
                      <div className="text-gray-500 text-[10px] font-mono">{item.sub}</div>
                    </div>
                  </div>
                ))}
              </div>

              <ul className="space-y-4 text-sm text-gray-300">
                {[
                  'Describe UI in natural language, ship pixel-perfect components',
                  'Refactor legacy code with a single AI instruction',
                  'Generate full REST API routes and database schemas from prompts',
                  'Debug errors by pasting stack traces â€” AI fixes and explains'
                ].map((item, idx) => (
                  <li key={idx} className="flex items-start">
                    <CheckCircle size={16} className="text-brand-cyan mr-3 mt-0.5 flex-shrink-0" />
                    <span>{item}</span>
                  </li>
                ))}
              </ul>

              {/* Language tags */}
              <div className="flex flex-wrap gap-2 mt-8">
                {['React', 'Next.js', 'TypeScript', 'Tailwind', 'Node.js', 'Python', 'SQL'].map(tag => (
                  <span key={tag} className="px-2.5 py-1 rounded-md text-[10px] font-mono font-semibold bg-white/5 border border-white/10 text-gray-400 hover:border-brand-cyan/30 hover:text-brand-cyan transition-all duration-200 cursor-default">{tag}</span>
                ))}
              </div>
            </div>

          </div>
        </section>
      );
    };

